// The Build 1 scripted scenario — the hand-written event log that stands in for live agents until
// Build 3 (brief §5).
export type { Agent } from './scenario';
export { AGENTS, agentName, MOCK_EVENTS } from './scenario';
