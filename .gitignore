target/
project/target/
project/project/
.bsp/
.bloop/
.metals/
.scala-build/
*.class
*.tasty
.idea/
.DS_Store

# local sample deals (public EDGAR pulls) — not committed
/samples/

# local env / secrets — never committed (keys live in ~/.config/llm/keys.env)
.envrc
.direnv/
*.env

# dev-ledger gate: hook-private state (per-clone, never committed)
ledger/.hook-signed
ledger/.pending-sign
ledger/__pycache__/

# composed-cell run artifacts (full event logs) — kept local, findings go in the docs
results/
