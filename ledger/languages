# Build languages of the system — extensions that fire the dev-ledger check.
# The gate runs ledger/check.sh whenever a staged file matches one of these, and
# ledger/check.sh must check every language listed here (else that language fails open).
# Declared explicitly (not auto-detected) because the TypeScript markers are nested
# under reasoning-society/frontend/, and the .py in the tree is the ledger harness
# (audit.py / gate.py), not part of the system's build.
#
# Scala backend (build.sbt at root):
.scala
.sc
.sbt
# TypeScript frontend (reasoning-society/frontend):
.ts
.tsx
