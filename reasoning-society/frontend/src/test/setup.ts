// Register the jest-dom matchers (toBeInTheDocument, toHaveTextContent, …) on
// Vitest's expect, and augment its Assertion types. Loaded once per test run via
// the `test.setupFiles` entry in vite.config.ts.
import '@testing-library/jest-dom/vitest';
