// The fold's public surface — the pure reducer, the gate's read, and the grade function (brief §1).
export {
  fold,
  answerSeqFor,
  signableCandidate,
  SIGN_THRESHOLD,
  MIN_CORROBORATORS,
} from './fold';
export { gradeOf } from './grade';
